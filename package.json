{
  "name": "root",
  "private": true,
  "workspaces": ["packages/*"],
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "run:blog": "cd packages/blog && yarn develop",
    "run:site": "cd packages/site && yarn develop",
    "run:all": "npm-run-all --parallel run:blog run:site"
  }
}
